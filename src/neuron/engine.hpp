#pragma once
#include "render/render_context.hpp"

#include <memory>

namespace neuron {

    class Engine final {
      public:
        Engine();
        ~Engine() = default;
      private:
    };

} // namespace neuron
